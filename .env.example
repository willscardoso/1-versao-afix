### Copy this file to `.env.local` and fill the values with your Supabase project keys.
# Do NOT commit `.env.local` with your real keys.

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: server-side service role key (keep secret, do NOT expose to browser)
SUPABASE_SERVICE_ROLE=your-service-role-key

# Other env vars the app may use can be added here.

# JWT secret used to sign session tokens (pick a long, random value)
JWT_SECRET=replace-with-a-long-random-secret
