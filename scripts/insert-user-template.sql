-- insert-user-template.sql
-- Use this as a template if you already have a bcrypt hash.
-- Replace the placeholders and run in Supabase SQL Editor (always backup first).

-- Example:
-- INSERT INTO public.users (email, password_hash, full_name, role, created_at, updated_at)
-- VALUES ('me@example.com', '$2a$10$...bcrypt-hash...', 'Full Name', 'client', now(), now());

-- Paste your statement below and run it.
